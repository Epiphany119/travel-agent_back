<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.travel</groupId>
        <artifactId>travel-agent</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>travel-web</artifactId>
    <name>travel-web</name>
    <description>Web 启动模块 - 聚合所有业务模块</description>

    <dependencies>
        <!-- 认证模块 -->
        <dependency>
            <groupId>com.travel</groupId>
            <artifactId>travel-module-auth-biz</artifactId>
        </dependency>

        <!-- 公共模块 -->
        <dependency>
            <groupId>com.travel</groupId>
            <artifactId>travel-common</artifactId>
        </dependency>

        <!-- Agent 模块 -->
        <dependency>
            <groupId>com.travel</groupId>
            <artifactId>travel-module-agent-biz</artifactId>
        </dependency>

        <!-- 行程模块 -->
        <dependency>
            <groupId>com.travel</groupId>
            <artifactId>travel-module-itinerary-biz</artifactId>
        </dependency>

        <!-- 用户模块 -->
        <dependency>
            <groupId>com.travel</groupId>
            <artifactId>travel-module-user-biz</artifactId>
        </dependency>

        <!-- A2A 运行时（Host Agent + 子 Agent + 编排器） -->
        <dependency>
            <groupId>com.travel</groupId>
            <artifactId>travel-a2a-runtime</artifactId>
        </dependency>

        <!-- MCP Server: Weather (8081) -->
        <dependency>
            <groupId>com.travel</groupId>
            <artifactId>travel-mcp-server-weather</artifactId>
        </dependency>

        <!-- MCP Server: POI (8082) -->
        <dependency>
            <groupId>com.travel</groupId>
            <artifactId>travel-mcp-server-poi</artifactId>
        </dependency>

        <!-- MCP Server: Meal (8083) -->
        <dependency>
            <groupId>com.travel</groupId>
            <artifactId>travel-mcp-server-meal</artifactId>
        </dependency>

        <!-- MCP Server: Budget (8084) -->
        <dependency>
            <groupId>com.travel</groupId>
            <artifactId>travel-mcp-server-budget</artifactId>
        </dependency>

        <!-- MySQL 驱动 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.travel.web.TravelWebApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
